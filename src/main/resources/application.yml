server:
  port: 8080

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/spofo
    driver-class-name: org.mariadb.jdbc.Driver
    username: khs
    password: 1234
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true # Jpa ?? ? SQL? ?? ??
    properties:
      hibernate:
        format_sql: true # ?? ??? ?? ????? ??

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace